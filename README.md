# global-thresholding-cv
Implementation of global thresholding using Otsu's method in computer vision. Python code for segmenting foreground objects from the background in images. Uses OpenCV and NumPy.
